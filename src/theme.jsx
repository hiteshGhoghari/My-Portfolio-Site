import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    typography: {
        fontFamily: "'Bai Jamjuree', sans-serif", // Change to your font
    },
});

export default theme;
